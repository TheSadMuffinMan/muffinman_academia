#include <iostream>

using namespace std;

void clearScreen();

// Function clears the screen
void clearScreen()
{
    // use "cls" in windows and "clear" command in Mac and Linux
    #ifdef _WIN32
        system("clS");
    #else
        system("clear");
    #endif
}