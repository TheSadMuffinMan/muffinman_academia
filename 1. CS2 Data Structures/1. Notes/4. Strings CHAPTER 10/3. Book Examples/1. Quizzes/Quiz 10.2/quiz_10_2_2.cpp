/* ***QUESTION 2***
Assume that s is a string. Write an expression whose value is true if and only if
the value of s would come between "mortgage" and "mortuary" in the dictionary.
*/

#include <iostream>
#include <string>

using namespace std;

int main(int argc, char *argv[])
{
    string s;

    return 0;
}